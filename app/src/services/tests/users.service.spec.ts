import request from 'supertest';

describe('User Service tests', () => {

    it('Deve criar um novo usuário na base de dados', async () => {
        const resposta = await request()
    });

    it('Deve retornar um erro quando o usuário já existir ao tentar criar um novo usuário na base de dados', async () => {
        
    });

    it('Deve listar todos os usuário na base de dados', async () => {
        
    });

    it('Deve listar usuário pelo id', async () => {
        
    });

    it('Deve retornar erro quando não encontrar o usuário ao tentar listar', async () => {
        
    });

    it('Deve atualizar o usuário pelo id', async () => {
        
    });

    it('Deve retornar erro quando não encontrar o usuário na base de dados para atualizar', async () => {
        
    });

    it('Deve deletar usuário pelo id', async () => {
        
    });

    it('Deve retornar erro quando não encontrar o usuário na base de dados para deletar', async () => {
        
    });

})