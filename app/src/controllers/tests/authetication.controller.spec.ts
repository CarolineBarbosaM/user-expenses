import request from 'supertest';

describe('Authentication Controller tests', () => {

    it('Deve fazer o login do usuário na base de dados', async () => {

    });

    it('Deve retornar um erro quando o usuário não puder fazer o login', async () => {
        
    });

})